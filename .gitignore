# Python
__pycache__/
*.pyc
*.pyo

# 运行时产物
uploads/*
!uploads/.gitkeep
server.log

# PyInstaller 构建产物
dist/
build/
*.spec

# SumatraPDF 引擎随仓库 bin/ 分发：内嵌于发布版 exe，也作为可选外置 bin/ 提供。
# 注意 bin/ 含二进制（约 22MB），会随仓库一起 git push（GitHub 仓库单文件硬限 100MB，安全）。

# 编辑器
.vscode/
.idea/
